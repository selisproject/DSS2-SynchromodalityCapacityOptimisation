// Copyright (c) CLMS. All rights reserved.
// Licensed under the AGPL-3.0 license. See LICENSE file in the project root for full license information.
// This source file was autogenerated by zAppDev(r).
var directiveScopeReadyLimit = 10;
var directiveScopeReadyTimeout = 200;
window.onbeforeunload = (e) => {
    if (window._context.isDirty) {
        var msg = window._resourcesManager.getGlobalResource("RES_WEBFORM_DirtyMessage");
        e.returnValue = msg;     // Gecko, Trident, Chrome 34+
        return msg;
    }
};
interface Window { scope_SignOutPage: DSS2_SynchromodalityCapacityOptimisation.Controllers.ISignOutPageScope; }
namespace DSS2_SynchromodalityCapacityOptimisation.Controllers {
const SignOutPageConditionalFormattings = [
        ];
const SignOutPageDataValidations = [
                                   ];
const SignOutPageCalculatedExpressions = [
        ];

// Datasets Column Info

// Datasets Projection Shemas


export interface ISignOutPageScope extends Joove.IWebPageScope {
model:
    DSS2_SynchromodalityCapacityOptimisation.ViewModels.SignOutPage.SignOutPageViewModel;
_partialModelStructure:
    any;
    _validations?: {
summary: Array<Joove.Validation.BindingEntry>,

    },
    _masterValidations?: any;
}

class SignOutPageController {


    private controllerActionFinished = (resetDirty: boolean) => {
        //window.scope_SignOutPage.model = window.scope_SignOutPage.dehydrate();
        //window.scope_MasterPage.model = window.scope_SignOutPage.model;
        DSS2_SynchromodalityCapacityOptimisation.ViewModels.SignOutPage.SignOutPageViewModel._lightCast(window.scope_SignOutPage.model);
        // Joove.Common.applyScope(window.scope_SignOutPage);
        if (resetDirty) {
            window._context.isDirty = false;
        }
        window._ruleEngine.update(Joove.EvaluationTimes.OnChange);
    }

    constructor(private $scope: ISignOutPageScope, $timeout: ng.ITimeoutService) {
        window.scope_SignOutPage = $scope;
        $scope.$onControlChanged = function (event, newValue, dontMakeDirty) {
            Joove.Core.onChange(event.target, newValue, dontMakeDirty)
        }

        $scope.dehydrate = (context = null) => DSS2_SynchromodalityCapacityOptimisation.ViewModels.SignOutPage.SignOutPageViewModel._initializeFrom($scope.model, context);
        $scope._validationsMaster =  window.scope_MasterPage._validations;
        $scope._validations = { summary:
                                [],
                              };

        this.init($scope, $timeout);
        $timeout(() => {

            window._ruleEngine.update(Joove.EvaluationTimes.OnLoad, null, () => {

                $("body").fadeIn(200);

            });
        });

    }

    init($scope: ISignOutPageScope, $timeout: ng.ITimeoutService) {


        $scope.model = DSS2_SynchromodalityCapacityOptimisation.ViewModels.SignOutPage.SignOutPageViewModel._initializeFrom(window.viewDTO.Model);

        window.scope_MasterPage.model = $scope.model;

        new Joove.ReferencesReconstructor().reconstructReferences($scope.model);
        $scope.trackObject = obj => Joove.Common.trackObject(obj);


        //}

        $.connection['eventsHub'].on('__connectedEvent');

        $.connection['eventsHub'].on('forcePageReload', () => {
            window.onbeforeunload = null;
            setTimeout(() => {
                window.location.reload();
            }, 3000);
        });
        $.connection.hub.start(() => {
            Joove.Common.getScope().connectedToSignals();
            Joove.Common.getMasterScope().connectedToSignals();

        });
        $scope.connectedToSignals = () => {
        }
// Event Listeners
        $scope.events = {
        };
        $scope.expressions = {
        }
// Dataset Handler
        $scope.datasets = {
        };
// Controller actions
        $scope.actions = {
SignOut:
            async (_cb, _modalOptions, _e) => {
                Joove.Common.autocompleteFix();


                Joove.Core.executeRedirectControllerAction("SignOutPage", "SignOut", "GET", [], {}, _modalOptions);
            },
Render:
            async (_cb, _modalOptions, _e) => {
                Joove.Common.autocompleteFix();

                const implementation = async (): Promise<any> => {
                    window["scope_SignOutPage"].model.Title = Joove.Common.nullSafe<any>(() => window._resourcesManager.getLocalResource("RES_PAGETITLE_Render", false), "");


                    // Joove.Common.applyScope(window.scope_SignOutPage);
                }


                let spamHelper = new Joove.SpamControlHelper(_e);
                spamHelper.Disable();
                await implementation();
                spamHelper.Enable();
                this.controllerActionFinished(true);
            },
        };
// Events
        $scope.eventCallbacks = {
        };

// Rules
        window._ruleEngine.addDataValidations(SignOutPageDataValidations);
        window._ruleEngine.addConditionalFormattings(SignOutPageConditionalFormattings);
        window._ruleEngine.addCalculatedExpressions(SignOutPageCalculatedExpressions);

        window._commander.executeCommands(window.viewDTO.ClientCommands);
        window.viewDTO.ClientCommands = [];

        Joove.Common.setNumberLocalizationSettings();
        Joove.DeveloperApi.init($scope as any, window.scope_MasterPage as any);
        window.$formExtend && window.$formExtend();
        window.$onFormLoaded && window.$onFormLoaded();
    }
}
angular.module("Application").controller("SignOutPageController", ["$scope", "$timeout", SignOutPageController] as Array<string>);
}
