// Copyright (c) CLMS. All rights reserved.
// Licensed under the AGPL-3.0 license. See LICENSE file in the project root for full license information.
// This source file was autogenerated by zAppDev(r).
using System;
using System.Runtime.Serialization;
using System.Collections.Generic;
using System.ComponentModel;
using System.Linq;
using System.Reflection;
using System.Collections;
using System.ComponentModel.DataAnnotations;
using System.Linq.Expressions;
using zAppDev.DotNet.Framework.Linq;
using zAppDev.DotNet.Framework.Data.Domain;
using zAppDev.DotNet.Framework.Workflow;
using zAppDev.DotNet.Framework.Identity.Model;

namespace DSS2_SynchromodalityCapacityOptimisation.BO
{
    /// <summary>
    /// The SLALeadTime class
    ///
    /// </summary>
    [Serializable]
    [DataContract]
    public class SLALeadTime : IDomainModelClass
    {
        #region SLALeadTime's Fields

        protected Guid _transientId= Guid.NewGuid();
        public virtual Guid TransientId
        {
            get
            {
                return _transientId;
            }
            set
            {
                _transientId = value;
            }
        }
        [DataMember(Name="Id")]
        protected int? id = 0;
        [DataMember(Name="AgreedLeadTime")]
        protected int? agreedLeadTime;
        [DataMember(Name="RealLeadTime")]
        protected int? realLeadTime;
        [DataMember(Name="Percentage")]
        protected decimal? percentage;
        [DataMember(Name="ItemDescription")]
        protected string itemDescription;
        [DataMember(Name="DateFormatted")]
        protected string dateFormatted;
        [DataMember(Name="Threshold")]
        protected decimal? threshold;
        [DataMember(Name="SLALeadDate")]
        protected DateTime? sLALeadDate;
        [DataMember(Name="Month")]
        protected int? month;
        [DataMember(Name="MonthlyDelayed")]
        protected decimal? monthlyDelayed;
        [DataMember(Name="MontlhyOrders")]
        protected decimal? montlhyOrders;
        [DataMember(Name="MonthlyPercentage")]
        protected decimal? monthlyPercentage;
        #endregion
        #region SLALeadTime's Properties
/// <summary>
/// The Id property
///
/// </summary>
///
        [Key]
        public virtual int? Id
        {
            get
            {
                return id;
            }
            set
            {
                id = value;
            }
        }
/// <summary>
/// The AgreedLeadTime property
///
/// </summary>
///
        public virtual int? AgreedLeadTime
        {
            get
            {
                return agreedLeadTime;
            }
            set
            {
                agreedLeadTime = value;
            }
        }
/// <summary>
/// The RealLeadTime property
///
/// </summary>
///
        public virtual int? RealLeadTime
        {
            get
            {
                return realLeadTime;
            }
            set
            {
                realLeadTime = value;
            }
        }
/// <summary>
/// The Percentage property
///
/// </summary>
///
        public virtual decimal? Percentage
        {
            get
            {
                return percentage;
            }
            set
            {
                percentage = value;
            }
        }
/// <summary>
/// The ItemDescription property
///
/// </summary>
///
        public virtual string ItemDescription
        {
            get
            {
                return itemDescription;
            }
            set
            {
                itemDescription = value;
            }
        }
/// <summary>
/// The DateFormatted property
///
/// </summary>
///
        public virtual string DateFormatted
        {
            get
            {
                return dateFormatted;
            }
            set
            {
                dateFormatted = value;
            }
        }
/// <summary>
/// The Threshold property
///
/// </summary>
///
        public virtual decimal? Threshold
        {
            get
            {
                return threshold;
            }
            set
            {
                threshold = value;
            }
        }
/// <summary>
/// The SLALeadDate property
///
/// </summary>
///
        public virtual DateTime? SLALeadDate
        {
            get
            {
                return sLALeadDate;
            }
            set
            {
                sLALeadDate = value;
            }
        }
/// <summary>
/// The Month property
///
/// </summary>
///
        public virtual int? Month
        {
            get
            {
                return month;
            }
            set
            {
                month = value;
            }
        }
/// <summary>
/// The MonthlyDelayed property
///
/// </summary>
///
        public virtual decimal? MonthlyDelayed
        {
            get
            {
                return monthlyDelayed;
            }
            set
            {
                monthlyDelayed = value;
            }
        }
/// <summary>
/// The MontlhyOrders property
///
/// </summary>
///
        public virtual decimal? MontlhyOrders
        {
            get
            {
                return montlhyOrders;
            }
            set
            {
                montlhyOrders = value;
            }
        }
/// <summary>
/// The MonthlyPercentage property
///
/// </summary>
///
        public virtual decimal? MonthlyPercentage
        {
            get
            {
                return monthlyPercentage;
            }
            set
            {
                monthlyPercentage = value;
            }
        }
        #endregion
        #region SLALeadTime's Participant Properties
        [DataMember(Name="Item")]
        protected Item item;
        public virtual Item Item
        {
            get
            {
                return item;
            }
            set
            {
                if(Equals(item, value)) return;
                var __oldValue = item;
                item?.InternalRemoveSLALeadTimes(this);
                item = value;
                if (value != null)
                {
                    item.InternalAddSLALeadTimes(this);
                }
            }
        }
        [DataMember(Name="Warehouse")]
        protected Warehouse warehouse;
        public virtual Warehouse Warehouse
        {
            get
            {
                return warehouse;
            }
            set
            {
                if(Equals(warehouse, value)) return;
                var __oldValue = warehouse;
                warehouse = value;
            }
        }
        #endregion
        #region Constructors
/// <summary>
/// Public constructors of the SLALeadTime class
/// </summary>
/// <returns>New SLALeadTime object</returns>
/// <remarks></remarks>
        public SLALeadTime() {}
        #endregion
        #region Methods

        public virtual List<string> _Validate(bool throwException = true)
        {
            var __errors = new List<string>();
            if (Id == null)
            {
                __errors.Add("Property 'Id' is required.");
            }
            if (ItemDescription != null && ItemDescription.Length > 100)
            {
                __errors.Add("Length of property 'ItemDescription' cannot be greater than 100.");
            }
            if (DateFormatted != null && DateFormatted.Length > 100)
            {
                __errors.Add("Length of property 'DateFormatted' cannot be greater than 100.");
            }
            if (throwException && __errors.Any())
            {
                throw new zAppDev.DotNet.Framework.Exceptions.BusinessException("An instance of TypeClass 'SLALeadTime' has validation errors:\r\n\r\n" + string.Join("\r\n", __errors));
            }
            return __errors;
        }

        public virtual int _GetUniqueIdentifier()
        {
            var hashCode = 399326290;
            hashCode = hashCode * -1521134295 + (Id?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (AgreedLeadTime?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (RealLeadTime?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Percentage?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (ItemDescription?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (DateFormatted?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Threshold?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (SLALeadDate?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Month?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (MonthlyDelayed?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (MontlhyOrders?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (MonthlyPercentage?.GetHashCode() ?? 0);
            return hashCode;
        }






/// <summary>
/// Copies the current object to a new instance
/// </summary>
/// <param name="deep">Copy members that refer to objects external to this class (not dependent)</param>
/// <param name="copiedObjects">Objects that should be reused</param>
/// <param name="asNew">Copy the current object as a new one, ready to be persisted, along all its members.</param>
/// <param name="reuseNestedObjects">If asNew is true, this flag if set, forces the reuse of all external objects.</param>
/// <param name="copy">Optional - An existing [SLALeadTime] instance to use as the destination.</param>
/// <returns>A copy of the object</returns>
        public virtual SLALeadTime Copy(bool deep=false, Hashtable copiedObjects=null, bool asNew=false, bool reuseNestedObjects = false, SLALeadTime copy = null)
        {
            if(copiedObjects == null)
            {
                copiedObjects = new Hashtable();
            }
            if (copy == null && copiedObjects.Contains(this))
                return (SLALeadTime)copiedObjects[this];
            copy = copy ?? new SLALeadTime();
            if (!asNew)
            {
                copy.TransientId = this.TransientId;
                copy.Id = this.Id;
            }
            copy.AgreedLeadTime = this.AgreedLeadTime;
            copy.RealLeadTime = this.RealLeadTime;
            copy.Percentage = this.Percentage;
            copy.ItemDescription = this.ItemDescription;
            copy.DateFormatted = this.DateFormatted;
            copy.Threshold = this.Threshold;
            copy.SLALeadDate = this.SLALeadDate;
            copy.Month = this.Month;
            copy.MonthlyDelayed = this.MonthlyDelayed;
            copy.MontlhyOrders = this.MontlhyOrders;
            copy.MonthlyPercentage = this.MonthlyPercentage;
            if (!copiedObjects.Contains(this))
            {
                copiedObjects.Add(this, copy);
            }
            if(deep && this.item != null)
            {
                if (!copiedObjects.Contains(this.item))
                {
                    if (asNew && reuseNestedObjects)
                        copy.Item = this.Item;
                    else if (asNew)
                        copy.Item = this.Item.Copy(deep, copiedObjects, true);
                    else
                        copy.item = this.item.Copy(deep, copiedObjects, false);
                }
                else
                {
                    if (asNew)
                        copy.Item = (Item)copiedObjects[this.Item];
                    else
                        copy.item = (Item)copiedObjects[this.Item];
                }
            }
            if(deep && this.warehouse != null)
            {
                if (!copiedObjects.Contains(this.warehouse))
                {
                    if (asNew && reuseNestedObjects)
                        copy.Warehouse = this.Warehouse;
                    else if (asNew)
                        copy.Warehouse = this.Warehouse.Copy(deep, copiedObjects, true);
                    else
                        copy.warehouse = this.warehouse.Copy(deep, copiedObjects, false);
                }
                else
                {
                    if (asNew)
                        copy.Warehouse = (Warehouse)copiedObjects[this.Warehouse];
                    else
                        copy.warehouse = (Warehouse)copiedObjects[this.Warehouse];
                }
            }
            return copy;
        }

        public override bool Equals(object obj)
        {
            var compareTo = obj as SLALeadTime;
            if (ReferenceEquals(this, compareTo))
            {
                return true;
            }
            if (compareTo == null || !this.GetType().Equals(compareTo.GetTypeUnproxied()))
            {
                return false;
            }
            if (this.HasSameNonDefaultIdAs(compareTo))
            {
                return true;
            }
            // Since the Ids aren't the same, both of them must be transient to
            // compare domain signatures; because if one is transient and the
            // other is a persisted entity, then they cannot be the same object.
            return this.IsTransient() && compareTo.IsTransient() && (base.Equals(compareTo) || this.TransientId.Equals(compareTo.TransientId));
        }

// Maintain equality operator semantics for entities.
        public static bool operator ==(SLALeadTime x, SLALeadTime y)
        {
            // By default, == and Equals compares references. In order to
            // maintain these semantics with entities, we need to compare by
            // identity value. The Equals(x, y) override is used to guard
            // against null values; it then calls EntityEquals().
            return Equals(x, y);
        }

// Maintain inequality operator semantics for entities.
        public static bool operator !=(SLALeadTime x, SLALeadTime y)
        {
            return !(x == y);
        }

        private PropertyInfo __propertyKeyCache;
        public virtual PropertyInfo GetPrimaryKey()
        {
            if (__propertyKeyCache == null)
            {
                __propertyKeyCache = this.GetType().GetProperty("Id");
            }
            return __propertyKeyCache;
        }


/// <summary>
///     To help ensure hashcode uniqueness, a carefully selected random number multiplier
///     is used within the calculation.  Goodrich and Tamassia's Data Structures and
///     Algorithms in Java asserts that 31, 33, 37, 39 and 41 will produce the fewest number
///     of collissions.  See http://computinglife.wordpress.com/2008/11/20/why-do-hash-functions-use-prime-numbers/
///     for more information.
/// </summary>
        private const int HashMultiplier = 31;
        private int? cachedHashcode;

        public override int GetHashCode()
        {
            if (this.cachedHashcode.HasValue)
            {
                return this.cachedHashcode.Value;
            }
            if (this.IsTransient())
            {
                //this.cachedHashcode = base.GetHashCode();
                return this.TransientId.GetHashCode(); //don't cache because this won't stay transient forever
            }
            else
            {
                unchecked
                {
                    // It's possible for two objects to return the same hash code based on
                    // identically valued properties, even if they're of two different types,
                    // so we include the object's type in the hash calculation
                    var hashCode = this.GetType().GetHashCode();
                    this.cachedHashcode = (hashCode * HashMultiplier) ^ this.Id.GetHashCode();
                }
            }
            return this.cachedHashcode.Value;
        }

/// <summary>
///     Transient objects are not associated with an item already in storage.  For instance,
///     a Customer is transient if its Id is 0.  It's virtual to allow NHibernate-backed
///     objects to be lazily loaded.
/// </summary>
        public virtual bool IsTransient()
        {
            return this.Id == default(int) || this.Id.Equals(default(int));
        }

/// <summary>
///     When NHibernate proxies objects, it masks the type of the actual entity object.
///     This wrapper burrows into the proxied object to get its actual type.
///
///     Although this assumes NHibernate is being used, it doesn't require any NHibernate
///     related dependencies and has no bad side effects if NHibernate isn't being used.
///
///     Related discussion is at http://groups.google.com/group/sharp-architecture/browse_thread/thread/ddd05f9baede023a ...thanks Jay Oliver!
/// </summary>
        protected virtual System.Type GetTypeUnproxied()
        {
            return this.GetType();
        }

/// <summary>
///     Returns true if self and the provided entity have the same Id values
///     and the Ids are not of the default Id value
/// </summary>
        protected bool HasSameNonDefaultIdAs(SLALeadTime compareTo)
        {
            return !this.IsTransient() && !compareTo.IsTransient() && this.Id.Equals(compareTo.Id);
        }

        #endregion


    }
}
