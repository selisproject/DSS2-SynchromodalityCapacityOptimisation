// Copyright (c) CLMS. All rights reserved.
// Licensed under the AGPL-3.0 license. See LICENSE file in the project root for full license information.
// This source file was autogenerated by zAppDev(r).
using System;
using System.Runtime.Serialization;
using System.Collections.Generic;
using System.ComponentModel;
using System.Linq;
using System.Reflection;
using System.Collections;
using System.ComponentModel.DataAnnotations;
using System.Linq.Expressions;
using zAppDev.DotNet.Framework.Linq;
using zAppDev.DotNet.Framework.Data.Domain;
using zAppDev.DotNet.Framework.Workflow;
using zAppDev.DotNet.Framework.Identity.Model;

namespace DSS2_SynchromodalityCapacityOptimisation.BO
{
    /// <summary>
    /// The Location class
    ///
    /// </summary>
    [Serializable]
    [DataContract]
    public class Location : IDomainModelClass
    {
        #region Location's Fields

        protected Guid _transientId= Guid.NewGuid();
        public virtual Guid TransientId
        {
            get
            {
                return _transientId;
            }
            set
            {
                _transientId = value;
            }
        }
        [DataMember(Name="Id")]
        protected int? id = 0;
        [DataMember(Name="Name")]
        protected string name;
        [DataMember(Name="Street")]
        protected string street;
        [DataMember(Name="City")]
        protected string city;
        [DataMember(Name="Country")]
        protected string country;
        [DataMember(Name="Number")]
        protected string number;
        [DataMember(Name="PostCode")]
        protected string postCode;
        [DataMember(Name="Label")]
        protected string label;
        [DataMember(Name="VersionTimestamp")]
        protected byte[] versionTimestamp;

#pragma warning disable 0649
        private bool disableInternalAdditions;
#pragma warning restore 0649
        #endregion
        #region Location's Properties
/// <summary>
/// The Id property
///
/// </summary>
///
        [Key]
        public virtual int? Id
        {
            get
            {
                return id;
            }
            set
            {
                id = value;
            }
        }
/// <summary>
/// The Name property
///
/// </summary>
///
        public virtual string Name
        {
            get
            {
                return name;
            }
            set
            {
                name = value;
            }
        }
/// <summary>
/// The Street property
///
/// </summary>
///
        public virtual string Street
        {
            get
            {
                return street;
            }
            set
            {
                street = value;
            }
        }
/// <summary>
/// The City property
///
/// </summary>
///
        public virtual string City
        {
            get
            {
                return city;
            }
            set
            {
                city = value;
            }
        }
/// <summary>
/// The Country property
///
/// </summary>
///
        public virtual string Country
        {
            get
            {
                return country;
            }
            set
            {
                country = value;
            }
        }
/// <summary>
/// The Number property
///
/// </summary>
///
        public virtual string Number
        {
            get
            {
                return number;
            }
            set
            {
                number = value;
            }
        }
/// <summary>
/// The PostCode property
///
/// </summary>
///
        public virtual string PostCode
        {
            get
            {
                return postCode;
            }
            set
            {
                postCode = value;
            }
        }
/// <summary>
/// The Label property
///
/// </summary>
///
        public virtual string Label
        {
            get
            {
                return label;
            }
            set
            {
                label = value;
            }
        }
/// <summary>
/// The VersionTimestamp property
///Provides concurrency control for the class
/// </summary>
///
        public virtual byte[] VersionTimestamp
        {
            get
            {
                return versionTimestamp;
            }
            set
            {
                versionTimestamp = value;
            }
        }
        #endregion
        #region Location's Participant Properties
        [DataMember(Name="Driver")]
        protected IList<Driver> driver = new List<Driver>();
        public virtual List<Driver> Driver
        {
            get
            {
                if (driver is Driver[])
                {
                    driver = driver.ToList();
                }
                if (driver == null)
                {
                    driver = new List<Driver>();
                }
                return driver.ToList();
            }
            set
            {
                if (driver is Driver[])
                {
                    driver = driver.ToList();
                }
                if (driver != null)
                {
                    var __itemsToDelete = new List<Driver>(driver);
                    foreach (var __item in __itemsToDelete)
                    {
                        RemoveDriver(__item);
                    }
                }
                if(value == null)
                {
                    driver = new List<Driver>();
                    return;
                }
                foreach(var __item in value)
                {
                    AddDriver(__item);
                }
            }
        }
        public virtual void AddDriver(IList<Driver> __items)
        {
            foreach (var __item in __items)
            {
                AddDriver(__item);
            }
        }

        public virtual void InternalAddDriver(Driver __item)
        {
            if (__item == null || disableInternalAdditions) return;
            driver?.Add(__item);
        }

        public virtual void InternalRemoveDriver(Driver __item)
        {
            if (__item == null) return;
            driver?.Remove(__item);
        }

        public virtual void AddDriver(Driver __item)
        {
            if (__item == null) return;
            if (__item.Employer != this)
                __item.Employer = this;
        }

        public virtual void AddAtIndexDriver(int index, Driver __item)
        {
            if (__item == null) return;
            driver?.Insert(index, __item);
            disableInternalAdditions = true;
            try
            {
                if (__item.Employer != this)
                    __item.Employer = this;
            }
            finally
            {
                disableInternalAdditions = false;
            }
        }

        public virtual void RemoveDriver(Driver __item)
        {
            if (__item != null)
            {
                __item.Employer = null;
            }
        }
        public virtual void SetDriverAt(Driver __item, int __index)
        {
            if (__item == null)
            {
                driver[__index].Employer = null;
            }
            else
            {
                driver[__index] = __item;
                if (__item.Employer != this)
                    __item.Employer = this;
            }
        }

        public virtual void ClearDriver()
        {
            if (driver!=null)
            {
                var __itemsToRemove = driver.ToList();
                foreach(var __item in __itemsToRemove)
                {
                    RemoveDriver(__item);
                }
            }
        }
        [DataMember(Name="Coordinates")]
        protected Coordinates coordinates;
        public virtual Coordinates Coordinates
        {
            get
            {
                return coordinates;
            }
            set
            {
                if(Equals(coordinates, value)) return;
                var __oldValue = coordinates;
                if (value != null)
                {
                    coordinates = value;
                }
                else
                {
                    if (coordinates != null)
                    {
                        coordinates = null;
                    }
                }
            }
        }
        #endregion
        #region Constructors
/// <summary>
/// Public constructors of the Location class
/// </summary>
/// <returns>New Location object</returns>
/// <remarks></remarks>
        public Location() {}
        #endregion
        #region Methods

        public virtual List<string> _Validate(bool throwException = true)
        {
            var __errors = new List<string>();
            if (Id == null)
            {
                __errors.Add("Property 'Id' is required.");
            }
            if (Name != null && Name.Length > 100)
            {
                __errors.Add("Length of property 'Name' cannot be greater than 100.");
            }
            if (Street != null && Street.Length > 100)
            {
                __errors.Add("Length of property 'Street' cannot be greater than 100.");
            }
            if (City != null && City.Length > 100)
            {
                __errors.Add("Length of property 'City' cannot be greater than 100.");
            }
            if (Country != null && Country.Length > 100)
            {
                __errors.Add("Length of property 'Country' cannot be greater than 100.");
            }
            if (Number != null && Number.Length > 100)
            {
                __errors.Add("Length of property 'Number' cannot be greater than 100.");
            }
            if (PostCode != null && PostCode.Length > 100)
            {
                __errors.Add("Length of property 'PostCode' cannot be greater than 100.");
            }
            if (Label != null && Label.Length > 100)
            {
                __errors.Add("Length of property 'Label' cannot be greater than 100.");
            }
            if (throwException && __errors.Any())
            {
                throw new zAppDev.DotNet.Framework.Exceptions.BusinessException("An instance of TypeClass 'Location' has validation errors:\r\n\r\n" + string.Join("\r\n", __errors));
            }
            return __errors;
        }

        public virtual int _GetUniqueIdentifier()
        {
            var hashCode = 399326290;
            hashCode = hashCode * -1521134295 + (Id?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Name?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Street?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (City?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Country?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Number?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (PostCode?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Label?.GetHashCode() ?? 0);
            return hashCode;
        }






/// <summary>
/// Copies the current object to a new instance
/// </summary>
/// <param name="deep">Copy members that refer to objects external to this class (not dependent)</param>
/// <param name="copiedObjects">Objects that should be reused</param>
/// <param name="asNew">Copy the current object as a new one, ready to be persisted, along all its members.</param>
/// <param name="reuseNestedObjects">If asNew is true, this flag if set, forces the reuse of all external objects.</param>
/// <param name="copy">Optional - An existing [Location] instance to use as the destination.</param>
/// <returns>A copy of the object</returns>
        public virtual Location Copy(bool deep=false, Hashtable copiedObjects=null, bool asNew=false, bool reuseNestedObjects = false, Location copy = null)
        {
            if(copiedObjects == null)
            {
                copiedObjects = new Hashtable();
            }
            if (copy == null && copiedObjects.Contains(this))
                return (Location)copiedObjects[this];
            copy = copy ?? new Location();
            if (!asNew)
            {
                copy.TransientId = this.TransientId;
                copy.Id = this.Id;
            }
            copy.Name = this.Name;
            copy.Street = this.Street;
            copy.City = this.City;
            copy.Country = this.Country;
            copy.Number = this.Number;
            copy.PostCode = this.PostCode;
            copy.Label = this.Label;
            if (!copiedObjects.Contains(this))
            {
                copiedObjects.Add(this, copy);
            }
            copy.driver = new List<Driver>();
            if(deep && this.driver != null)
            {
                foreach (var __item in this.driver)
                {
                    if (!copiedObjects.Contains(__item))
                    {
                        if (asNew && reuseNestedObjects)
                            copy.AddDriver(__item);
                        else
                            copy.AddDriver(__item.Copy(deep, copiedObjects, asNew));
                    }
                    else
                    {
                        copy.AddDriver((Driver)copiedObjects[__item]);
                    }
                }
            }
            if(deep && this.coordinates != null)
            {
                if (!copiedObjects.Contains(this.coordinates))
                {
                    if (asNew && reuseNestedObjects)
                        copy.Coordinates = this.Coordinates;
                    else if (asNew)
                        copy.Coordinates = this.Coordinates.Copy(deep, copiedObjects, true);
                    else
                        copy.coordinates = this.coordinates.Copy(deep, copiedObjects, false);
                }
                else
                {
                    if (asNew)
                        copy.Coordinates = (Coordinates)copiedObjects[this.Coordinates];
                    else
                        copy.coordinates = (Coordinates)copiedObjects[this.Coordinates];
                }
            }
            return copy;
        }

        public override bool Equals(object obj)
        {
            var compareTo = obj as Location;
            if (ReferenceEquals(this, compareTo))
            {
                return true;
            }
            if (compareTo == null || !this.GetType().Equals(compareTo.GetTypeUnproxied()))
            {
                return false;
            }
            if (this.HasSameNonDefaultIdAs(compareTo))
            {
                return true;
            }
            // Since the Ids aren't the same, both of them must be transient to
            // compare domain signatures; because if one is transient and the
            // other is a persisted entity, then they cannot be the same object.
            return this.IsTransient() && compareTo.IsTransient() && (base.Equals(compareTo) || this.TransientId.Equals(compareTo.TransientId));
        }

// Maintain equality operator semantics for entities.
        public static bool operator ==(Location x, Location y)
        {
            // By default, == and Equals compares references. In order to
            // maintain these semantics with entities, we need to compare by
            // identity value. The Equals(x, y) override is used to guard
            // against null values; it then calls EntityEquals().
            return Equals(x, y);
        }

// Maintain inequality operator semantics for entities.
        public static bool operator !=(Location x, Location y)
        {
            return !(x == y);
        }

        private PropertyInfo __propertyKeyCache;
        public virtual PropertyInfo GetPrimaryKey()
        {
            if (__propertyKeyCache == null)
            {
                __propertyKeyCache = this.GetType().GetProperty("Id");
            }
            return __propertyKeyCache;
        }


/// <summary>
///     To help ensure hashcode uniqueness, a carefully selected random number multiplier
///     is used within the calculation.  Goodrich and Tamassia's Data Structures and
///     Algorithms in Java asserts that 31, 33, 37, 39 and 41 will produce the fewest number
///     of collissions.  See http://computinglife.wordpress.com/2008/11/20/why-do-hash-functions-use-prime-numbers/
///     for more information.
/// </summary>
        private const int HashMultiplier = 31;
        private int? cachedHashcode;

        public override int GetHashCode()
        {
            if (this.cachedHashcode.HasValue)
            {
                return this.cachedHashcode.Value;
            }
            if (this.IsTransient())
            {
                //this.cachedHashcode = base.GetHashCode();
                return this.TransientId.GetHashCode(); //don't cache because this won't stay transient forever
            }
            else
            {
                unchecked
                {
                    // It's possible for two objects to return the same hash code based on
                    // identically valued properties, even if they're of two different types,
                    // so we include the object's type in the hash calculation
                    var hashCode = this.GetType().GetHashCode();
                    this.cachedHashcode = (hashCode * HashMultiplier) ^ this.Id.GetHashCode();
                }
            }
            return this.cachedHashcode.Value;
        }

/// <summary>
///     Transient objects are not associated with an item already in storage.  For instance,
///     a Customer is transient if its Id is 0.  It's virtual to allow NHibernate-backed
///     objects to be lazily loaded.
/// </summary>
        public virtual bool IsTransient()
        {
            return this.Id == default(int) || this.Id.Equals(default(int));
        }

/// <summary>
///     When NHibernate proxies objects, it masks the type of the actual entity object.
///     This wrapper burrows into the proxied object to get its actual type.
///
///     Although this assumes NHibernate is being used, it doesn't require any NHibernate
///     related dependencies and has no bad side effects if NHibernate isn't being used.
///
///     Related discussion is at http://groups.google.com/group/sharp-architecture/browse_thread/thread/ddd05f9baede023a ...thanks Jay Oliver!
/// </summary>
        protected virtual System.Type GetTypeUnproxied()
        {
            return this.GetType();
        }

/// <summary>
///     Returns true if self and the provided entity have the same Id values
///     and the Ids are not of the default Id value
/// </summary>
        protected bool HasSameNonDefaultIdAs(Location compareTo)
        {
            return !this.IsTransient() && !compareTo.IsTransient() && this.Id.Equals(compareTo.Id);
        }

        #endregion


    }
}
