// Copyright (c) CLMS. All rights reserved.
// Licensed under the AGPL-3.0 license. See LICENSE file in the project root for full license information.
// This source file was autogenerated by zAppDev(r).
using System;
using System.Runtime.Serialization;
using System.Collections.Generic;
using System.ComponentModel;
using System.Linq;
using System.Reflection;
using System.Collections;
using System.ComponentModel.DataAnnotations;
using System.Linq.Expressions;
using zAppDev.DotNet.Framework.Linq;
using zAppDev.DotNet.Framework.Data.Domain;
using zAppDev.DotNet.Framework.Workflow;
using zAppDev.DotNet.Framework.Identity.Model;

namespace DSS2_SynchromodalityCapacityOptimisation.BO
{
    /// <summary>
    /// The tblZslTransportEvent class
    ///
    /// </summary>
    [Serializable]
    [DataContract]
    public class tblZslTransportEvent : IDomainModelClass
    {
        #region tblZslTransportEvent's Fields

        protected Guid _transientId= Guid.NewGuid();
        public virtual Guid TransientId
        {
            get
            {
                return _transientId;
            }
            set
            {
                _transientId = value;
            }
        }
        [DataMember(Name="EventID")]
        protected long? eventID = 0L;
        [DataMember(Name="EventDesc")]
        protected string eventDesc;
        [DataMember(Name="EventTime")]
        protected DateTime? eventTime;
        [DataMember(Name="VersionTimestamp")]
        protected byte[] versionTimestamp;

        #endregion
        #region tblZslTransportEvent's Properties
/// <summary>
/// The EventID property
///
/// </summary>
///
        [Key]
        public virtual long? EventID
        {
            get
            {
                return eventID;
            }
            set
            {
                eventID = value;
            }
        }
/// <summary>
/// The EventDesc property
///
/// </summary>
///
        public virtual string EventDesc
        {
            get
            {
                return eventDesc;
            }
            set
            {
                eventDesc = value;
            }
        }
/// <summary>
/// The EventTime property
///
/// </summary>
///
        public virtual DateTime? EventTime
        {
            get
            {
                return eventTime;
            }
            set
            {
                eventTime = value;
            }
        }
/// <summary>
/// The VersionTimestamp property
///Provides concurrency control for the class
/// </summary>
///
        public virtual byte[] VersionTimestamp
        {
            get
            {
                return versionTimestamp;
            }
            set
            {
                versionTimestamp = value;
            }
        }
        #endregion
        #region tblZslTransportEvent's Participant Properties
        [DataMember(Name="tblZslCustomer")]
        protected tblZslCustomer _tblZslCustomer;
        public virtual tblZslCustomer tblZslCustomer
        {
            get
            {
                return _tblZslCustomer;
            }
            set
            {
                if(Equals(_tblZslCustomer, value)) return;
                var __oldValue = _tblZslCustomer;
                if (value != null)
                {
                    _tblZslCustomer = value;
                }
                else
                {
                    if (_tblZslCustomer != null)
                    {
                        _tblZslCustomer = null;
                    }
                }
            }
        }
        [DataMember(Name="tblZslWorker")]
        protected tblZslWorker _tblZslWorker;
        public virtual tblZslWorker tblZslWorker
        {
            get
            {
                return _tblZslWorker;
            }
            set
            {
                if(Equals(_tblZslWorker, value)) return;
                var __oldValue = _tblZslWorker;
                if (value != null)
                {
                    _tblZslWorker = value;
                }
                else
                {
                    if (_tblZslWorker != null)
                    {
                        _tblZslWorker = null;
                    }
                }
            }
        }
        #endregion
        #region Constructors
/// <summary>
/// Public constructors of the tblZslTransportEvent class
/// </summary>
/// <returns>New tblZslTransportEvent object</returns>
/// <remarks></remarks>
        public tblZslTransportEvent() {}
        #endregion
        #region Methods

        public virtual List<string> _Validate(bool throwException = true)
        {
            var __errors = new List<string>();
            if (EventID == null)
            {
                __errors.Add("Property 'EventID' is required.");
            }
            if (EventDesc != null && EventDesc.Length > 100)
            {
                __errors.Add("Length of property 'EventDesc' cannot be greater than 100.");
            }
            if (throwException && __errors.Any())
            {
                throw new zAppDev.DotNet.Framework.Exceptions.BusinessException("An instance of TypeClass 'tblZslTransportEvent' has validation errors:\r\n\r\n" + string.Join("\r\n", __errors));
            }
            return __errors;
        }

        public virtual int _GetUniqueIdentifier()
        {
            var hashCode = 399326290;
            hashCode = hashCode * -1521134295 + (EventID?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (EventDesc?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (EventTime?.GetHashCode() ?? 0);
            return hashCode;
        }






/// <summary>
/// Copies the current object to a new instance
/// </summary>
/// <param name="deep">Copy members that refer to objects external to this class (not dependent)</param>
/// <param name="copiedObjects">Objects that should be reused</param>
/// <param name="asNew">Copy the current object as a new one, ready to be persisted, along all its members.</param>
/// <param name="reuseNestedObjects">If asNew is true, this flag if set, forces the reuse of all external objects.</param>
/// <param name="copy">Optional - An existing [tblZslTransportEvent] instance to use as the destination.</param>
/// <returns>A copy of the object</returns>
        public virtual tblZslTransportEvent Copy(bool deep=false, Hashtable copiedObjects=null, bool asNew=false, bool reuseNestedObjects = false, tblZslTransportEvent copy = null)
        {
            if(copiedObjects == null)
            {
                copiedObjects = new Hashtable();
            }
            if (copy == null && copiedObjects.Contains(this))
                return (tblZslTransportEvent)copiedObjects[this];
            copy = copy ?? new tblZslTransportEvent();
            if (!asNew)
            {
                copy.TransientId = this.TransientId;
                copy.EventID = this.EventID;
            }
            copy.EventDesc = this.EventDesc;
            copy.EventTime = this.EventTime;
            if (!copiedObjects.Contains(this))
            {
                copiedObjects.Add(this, copy);
            }
            if(deep && this._tblZslCustomer != null)
            {
                if (!copiedObjects.Contains(this._tblZslCustomer))
                {
                    if (asNew && reuseNestedObjects)
                        copy.tblZslCustomer = this.tblZslCustomer;
                    else if (asNew)
                        copy.tblZslCustomer = this.tblZslCustomer.Copy(deep, copiedObjects, true);
                    else
                        copy._tblZslCustomer = this._tblZslCustomer.Copy(deep, copiedObjects, false);
                }
                else
                {
                    if (asNew)
                        copy.tblZslCustomer = (tblZslCustomer)copiedObjects[this.tblZslCustomer];
                    else
                        copy._tblZslCustomer = (tblZslCustomer)copiedObjects[this.tblZslCustomer];
                }
            }
            if(deep && this._tblZslWorker != null)
            {
                if (!copiedObjects.Contains(this._tblZslWorker))
                {
                    if (asNew && reuseNestedObjects)
                        copy.tblZslWorker = this.tblZslWorker;
                    else if (asNew)
                        copy.tblZslWorker = this.tblZslWorker.Copy(deep, copiedObjects, true);
                    else
                        copy._tblZslWorker = this._tblZslWorker.Copy(deep, copiedObjects, false);
                }
                else
                {
                    if (asNew)
                        copy.tblZslWorker = (tblZslWorker)copiedObjects[this.tblZslWorker];
                    else
                        copy._tblZslWorker = (tblZslWorker)copiedObjects[this.tblZslWorker];
                }
            }
            return copy;
        }

        public override bool Equals(object obj)
        {
            var compareTo = obj as tblZslTransportEvent;
            if (ReferenceEquals(this, compareTo))
            {
                return true;
            }
            if (compareTo == null || !this.GetType().Equals(compareTo.GetTypeUnproxied()))
            {
                return false;
            }
            if (this.HasSameNonDefaultIdAs(compareTo))
            {
                return true;
            }
            // Since the Ids aren't the same, both of them must be transient to
            // compare domain signatures; because if one is transient and the
            // other is a persisted entity, then they cannot be the same object.
            return this.IsTransient() && compareTo.IsTransient() && (base.Equals(compareTo) || this.TransientId.Equals(compareTo.TransientId));
        }

// Maintain equality operator semantics for entities.
        public static bool operator ==(tblZslTransportEvent x, tblZslTransportEvent y)
        {
            // By default, == and Equals compares references. In order to
            // maintain these semantics with entities, we need to compare by
            // identity value. The Equals(x, y) override is used to guard
            // against null values; it then calls EntityEquals().
            return Equals(x, y);
        }

// Maintain inequality operator semantics for entities.
        public static bool operator !=(tblZslTransportEvent x, tblZslTransportEvent y)
        {
            return !(x == y);
        }

        private PropertyInfo __propertyKeyCache;
        public virtual PropertyInfo GetPrimaryKey()
        {
            if (__propertyKeyCache == null)
            {
                __propertyKeyCache = this.GetType().GetProperty("EventID");
            }
            return __propertyKeyCache;
        }


/// <summary>
///     To help ensure hashcode uniqueness, a carefully selected random number multiplier
///     is used within the calculation.  Goodrich and Tamassia's Data Structures and
///     Algorithms in Java asserts that 31, 33, 37, 39 and 41 will produce the fewest number
///     of collissions.  See http://computinglife.wordpress.com/2008/11/20/why-do-hash-functions-use-prime-numbers/
///     for more information.
/// </summary>
        private const int HashMultiplier = 31;
        private int? cachedHashcode;

        public override int GetHashCode()
        {
            if (this.cachedHashcode.HasValue)
            {
                return this.cachedHashcode.Value;
            }
            if (this.IsTransient())
            {
                //this.cachedHashcode = base.GetHashCode();
                return this.TransientId.GetHashCode(); //don't cache because this won't stay transient forever
            }
            else
            {
                unchecked
                {
                    // It's possible for two objects to return the same hash code based on
                    // identically valued properties, even if they're of two different types,
                    // so we include the object's type in the hash calculation
                    var hashCode = this.GetType().GetHashCode();
                    this.cachedHashcode = (hashCode * HashMultiplier) ^ this.EventID.GetHashCode();
                }
            }
            return this.cachedHashcode.Value;
        }

/// <summary>
///     Transient objects are not associated with an item already in storage.  For instance,
///     a Customer is transient if its Id is 0.  It's virtual to allow NHibernate-backed
///     objects to be lazily loaded.
/// </summary>
        public virtual bool IsTransient()
        {
            return this.EventID == default(long) || this.EventID.Equals(default(long));
        }

/// <summary>
///     When NHibernate proxies objects, it masks the type of the actual entity object.
///     This wrapper burrows into the proxied object to get its actual type.
///
///     Although this assumes NHibernate is being used, it doesn't require any NHibernate
///     related dependencies and has no bad side effects if NHibernate isn't being used.
///
///     Related discussion is at http://groups.google.com/group/sharp-architecture/browse_thread/thread/ddd05f9baede023a ...thanks Jay Oliver!
/// </summary>
        protected virtual System.Type GetTypeUnproxied()
        {
            return this.GetType();
        }

/// <summary>
///     Returns true if self and the provided entity have the same Id values
///     and the Ids are not of the default Id value
/// </summary>
        protected bool HasSameNonDefaultIdAs(tblZslTransportEvent compareTo)
        {
            return !this.IsTransient() && !compareTo.IsTransient() && this.EventID.Equals(compareTo.EventID);
        }

        #endregion


    }
}
