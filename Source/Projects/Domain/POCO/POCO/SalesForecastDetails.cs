// Copyright (c) CLMS. All rights reserved.
// Licensed under the AGPL-3.0 license. See LICENSE file in the project root for full license information.
// This source file was autogenerated by zAppDev(r).
using System;
using System.Runtime.Serialization;
using System.Collections.Generic;
using System.ComponentModel;
using System.Linq;
using System.Reflection;
using System.Collections;
using System.ComponentModel.DataAnnotations;
using System.Linq.Expressions;
using zAppDev.DotNet.Framework.Linq;
using zAppDev.DotNet.Framework.Data.Domain;
using zAppDev.DotNet.Framework.Workflow;
using zAppDev.DotNet.Framework.Identity.Model;

namespace DSS2_SynchromodalityCapacityOptimisation.BO
{
    /// <summary>
    /// The SalesForecastDetails class
    ///
    /// </summary>
    [Serializable]
    [DataContract]
    [KnownType(typeof(Sales))]

    public class SalesForecastDetails : Sales, IDomainModelClass
    {
        #region SalesForecastDetails's Fields
        [DataMember(Name="AvgDailyDemandForecast")]
        protected decimal? avgDailyDemandForecast;
        [DataMember(Name="ComputedStock")]
        protected int? computedStock;
        [DataMember(Name="NewOrder")]
        protected int? newOrder;
        #endregion
        #region SalesForecastDetails's Properties
/// <summary>
/// The AvgDailyDemandForecast property
///
/// </summary>
///
        public virtual decimal? AvgDailyDemandForecast
        {
            get
            {
                return avgDailyDemandForecast;
            }
            set
            {
                avgDailyDemandForecast = value;
            }
        }
/// <summary>
/// The ComputedStock property
///
/// </summary>
///
        public virtual int? ComputedStock
        {
            get
            {
                return computedStock;
            }
            set
            {
                computedStock = value;
            }
        }
/// <summary>
/// The NewOrder property
///
/// </summary>
///
        public virtual int? NewOrder
        {
            get
            {
                return newOrder;
            }
            set
            {
                newOrder = value;
            }
        }
        #endregion
        #region SalesForecastDetails's Participant Properties
        [DataMember(Name="SalesForecast")]
        protected SalesForecast salesForecast;
        public virtual SalesForecast SalesForecast
        {
            get
            {
                return salesForecast;
            }
            set
            {
                if(Equals(salesForecast, value)) return;
                var __oldValue = salesForecast;
                salesForecast?.InternalRemoveSalesForecastDetails(this);
                salesForecast = value;
                if (value != null)
                {
                    salesForecast.InternalAddSalesForecastDetails(this);
                }
            }
        }
        #endregion
        #region Constructors
/// <summary>
/// Public constructors of the SalesForecastDetails class
/// </summary>
/// <returns>New SalesForecastDetails object</returns>
/// <remarks></remarks>
        public SalesForecastDetails(): base()
        {
            computedStock = 0;
            newOrder = 0;
        }
        #endregion
        #region Methods

        public override List<string> _Validate(bool throwException = true)
        {
            var __errors = new List<string>();
            __errors = base._Validate(throwException);
            if (throwException && __errors.Any())
            {
                throw new zAppDev.DotNet.Framework.Exceptions.BusinessException("An instance of TypeClass 'SalesForecastDetails' has validation errors:\r\n\r\n" + string.Join("\r\n", __errors));
            }
            return __errors;
        }

        public virtual int _GetUniqueIdentifier()
        {
            var hashCode = 399326290;
            hashCode = hashCode * -1521134295 + (AvgDailyDemandForecast?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (ComputedStock?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (NewOrder?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Id?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (ForecastDate?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Units?.GetHashCode() ?? 0);
            return hashCode;
        }






/// <summary>
/// Copies the current object to a new instance
/// </summary>
/// <param name="deep">Copy members that refer to objects external to this class (not dependent)</param>
/// <param name="copiedObjects">Objects that should be reused</param>
/// <param name="asNew">Copy the current object as a new one, ready to be persisted, along all its members.</param>
/// <param name="reuseNestedObjects">If asNew is true, this flag if set, forces the reuse of all external objects.</param>
/// <param name="copy">Optional - An existing [SalesForecastDetails] instance to use as the destination.</param>
/// <returns>A copy of the object</returns>
        public virtual SalesForecastDetails Copy(bool deep=false, Hashtable copiedObjects=null, bool asNew=false, bool reuseNestedObjects = false, SalesForecastDetails copy = null)
        {
            if(copiedObjects == null)
            {
                copiedObjects = new Hashtable();
            }
            if (copy == null && copiedObjects.Contains(this))
                return (SalesForecastDetails)copiedObjects[this];
            copy = copy ?? new SalesForecastDetails();
            if (!asNew)
            {
                copy.TransientId = this.TransientId;
            }
            copy.AvgDailyDemandForecast = this.AvgDailyDemandForecast;
            copy.ComputedStock = this.ComputedStock;
            copy.NewOrder = this.NewOrder;
            if (!copiedObjects.Contains(this))
            {
                copiedObjects.Add(this, copy);
            }
            if(deep && this.salesForecast != null)
            {
                if (!copiedObjects.Contains(this.salesForecast))
                {
                    if (asNew && reuseNestedObjects)
                        copy.SalesForecast = this.SalesForecast;
                    else if (asNew)
                        copy.SalesForecast = this.SalesForecast.Copy(deep, copiedObjects, true);
                    else
                        copy.salesForecast = this.salesForecast.Copy(deep, copiedObjects, false);
                }
                else
                {
                    if (asNew)
                        copy.SalesForecast = (SalesForecast)copiedObjects[this.SalesForecast];
                    else
                        copy.salesForecast = (SalesForecast)copiedObjects[this.SalesForecast];
                }
            }
            base.Copy(deep, copiedObjects, asNew, reuseNestedObjects, copy);
            return copy;
        }

        public override bool Equals(object obj)
        {
            var compareTo = obj as SalesForecastDetails;
            if (ReferenceEquals(this, compareTo))
            {
                return true;
            }
            if (compareTo == null || !this.GetType().Equals(compareTo.GetTypeUnproxied()))
            {
                return false;
            }
            if (this.HasSameNonDefaultIdAs(compareTo))
            {
                return true;
            }
            // Since the Ids aren't the same, both of them must be transient to
            // compare domain signatures; because if one is transient and the
            // other is a persisted entity, then they cannot be the same object.
            return this.IsTransient() && compareTo.IsTransient() && (base.Equals(compareTo) || this.TransientId.Equals(compareTo.TransientId));
        }

// Maintain equality operator semantics for entities.
        public static bool operator ==(SalesForecastDetails x, SalesForecastDetails y)
        {
            // By default, == and Equals compares references. In order to
            // maintain these semantics with entities, we need to compare by
            // identity value. The Equals(x, y) override is used to guard
            // against null values; it then calls EntityEquals().
            return Equals(x, y);
        }

// Maintain inequality operator semantics for entities.
        public static bool operator !=(SalesForecastDetails x, SalesForecastDetails y)
        {
            return !(x == y);
        }

        public override int GetHashCode()
        {
            return base.GetHashCode();
        }

        #endregion


    }
}
