// Copyright (c) CLMS. All rights reserved.
// Licensed under the AGPL-3.0 license. See LICENSE file in the project root for full license information.
// This source file was autogenerated by zAppDev(r).
using System;
using System.Runtime.Serialization;
using System.Collections.Generic;
using System.ComponentModel;
using System.Linq;
using System.Reflection;
using System.Collections;
using System.ComponentModel.DataAnnotations;
using System.Linq.Expressions;
using zAppDev.DotNet.Framework.Linq;
using zAppDev.DotNet.Framework.Data.Domain;
using zAppDev.DotNet.Framework.Workflow;
using zAppDev.DotNet.Framework.Identity.Model;

namespace DSS2_SynchromodalityCapacityOptimisation.BO
{
    /// <summary>
    /// The ProofOfDelivery class
    ///
    /// </summary>
    [Serializable]
    [DataContract]
    public class ProofOfDelivery : IDomainModelClass
    {
        #region ProofOfDelivery's Fields

        protected Guid _transientId= Guid.NewGuid();
        public virtual Guid TransientId
        {
            get
            {
                return _transientId;
            }
            set
            {
                _transientId = value;
            }
        }
        [DataMember(Name="Id")]
        protected int? id = 0;
        [DataMember(Name="DateIssued")]
        protected DateTime? dateIssued;
        [DataMember(Name="PODNumber")]
        protected long? pODNumber;
        [DataMember(Name="QualityStatus")]
        protected string qualityStatus;
        [DataMember(Name="QuantityStatus")]
        protected string quantityStatus;
        [DataMember(Name="DeliveryStatus")]
        protected string deliveryStatus;
        [DataMember(Name="PODAttachment")]
        protected string pODAttachment;
        [DataMember(Name="Comments")]
        protected string comments;
        [DataMember(Name="DamagedPackaging")]
        protected bool damagedPackaging;
        [DataMember(Name="DamagedProducts")]
        protected bool damagedProducts;
        [DataMember(Name="CloseToExpiry")]
        protected bool closeToExpiry;
        [DataMember(Name="VersionTimestamp")]
        protected byte[] versionTimestamp;

        #endregion
        #region ProofOfDelivery's Properties
/// <summary>
/// The Id property
///
/// </summary>
///
        [Key]
        public virtual int? Id
        {
            get
            {
                return id;
            }
            set
            {
                id = value;
            }
        }
/// <summary>
/// The DateIssued property
///
/// </summary>
///
        public virtual DateTime? DateIssued
        {
            get
            {
                return dateIssued;
            }
            set
            {
                dateIssued = value;
            }
        }
/// <summary>
/// The PODNumber property
///
/// </summary>
///
        public virtual long? PODNumber
        {
            get
            {
                return pODNumber;
            }
            set
            {
                pODNumber = value;
            }
        }
/// <summary>
/// The QualityStatus property
///
/// </summary>
///
        public virtual string QualityStatus
        {
            get
            {
                return qualityStatus;
            }
            set
            {
                qualityStatus = value;
            }
        }
/// <summary>
/// The QuantityStatus property
///
/// </summary>
///
        public virtual string QuantityStatus
        {
            get
            {
                return quantityStatus;
            }
            set
            {
                quantityStatus = value;
            }
        }
/// <summary>
/// The DeliveryStatus property
///
/// </summary>
///
        public virtual string DeliveryStatus
        {
            get
            {
                return deliveryStatus;
            }
            set
            {
                deliveryStatus = value;
            }
        }
/// <summary>
/// The PODAttachment property
///
/// </summary>
///
        public virtual string PODAttachment
        {
            get
            {
                return pODAttachment;
            }
            set
            {
                pODAttachment = value;
            }
        }
/// <summary>
/// The Comments property
///
/// </summary>
///
        public virtual string Comments
        {
            get
            {
                return comments;
            }
            set
            {
                comments = value;
            }
        }
/// <summary>
/// The DamagedPackaging property
///
/// </summary>
///
        public virtual bool DamagedPackaging
        {
            get
            {
                return damagedPackaging;
            }
            set
            {
                damagedPackaging = value;
            }
        }
/// <summary>
/// The DamagedProducts property
///
/// </summary>
///
        public virtual bool DamagedProducts
        {
            get
            {
                return damagedProducts;
            }
            set
            {
                damagedProducts = value;
            }
        }
/// <summary>
/// The CloseToExpiry property
///
/// </summary>
///
        public virtual bool CloseToExpiry
        {
            get
            {
                return closeToExpiry;
            }
            set
            {
                closeToExpiry = value;
            }
        }
/// <summary>
/// The VersionTimestamp property
///Provides concurrency control for the class
/// </summary>
///
        public virtual byte[] VersionTimestamp
        {
            get
            {
                return versionTimestamp;
            }
            set
            {
                versionTimestamp = value;
            }
        }
        #endregion
        #region ProofOfDelivery's Participant Properties
        [DataMember(Name="TruckDriver")]
        protected TruckDriver truckDriver;
        public virtual TruckDriver TruckDriver
        {
            get
            {
                return truckDriver;
            }
            set
            {
                if(Equals(truckDriver, value)) return;
                var __oldValue = truckDriver;
                truckDriver?.InternalRemoveProofOfDelivery(this);
                truckDriver = value;
                if (value != null)
                {
                    truckDriver.InternalAddProofOfDelivery(this);
                }
            }
        }
        [DataMember(Name="OrderWMS")]
        protected OrderWMS orderWMS;
        public virtual OrderWMS OrderWMS
        {
            get
            {
                return orderWMS;
            }
            set
            {
                if(Equals(orderWMS, value)) return;
                var __oldValue = orderWMS;
                if (value != null)
                {
                    if(orderWMS != null && !Equals(orderWMS, value))
                        orderWMS.ProofOfDelivery = null;
                    orderWMS = value;
                    if(orderWMS.ProofOfDelivery != this)
                        orderWMS.ProofOfDelivery = this;
                }
                else
                {
                    if (orderWMS != null)
                    {
                        var __obj = orderWMS;
                        orderWMS = null;
                        __obj.ProofOfDelivery = null;
                    }
                }
            }
        }
        #endregion
        #region Constructors
/// <summary>
/// Public constructors of the ProofOfDelivery class
/// </summary>
/// <returns>New ProofOfDelivery object</returns>
/// <remarks></remarks>
        public ProofOfDelivery() {}
        #endregion
        #region Methods

        public virtual List<string> _Validate(bool throwException = true)
        {
            var __errors = new List<string>();
            if (Id == null)
            {
                __errors.Add("Property 'Id' is required.");
            }
            if (QualityStatus != null && QualityStatus.Length > 100)
            {
                __errors.Add("Length of property 'QualityStatus' cannot be greater than 100.");
            }
            if (QuantityStatus != null && QuantityStatus.Length > 100)
            {
                __errors.Add("Length of property 'QuantityStatus' cannot be greater than 100.");
            }
            if (DeliveryStatus != null && DeliveryStatus.Length > 100)
            {
                __errors.Add("Length of property 'DeliveryStatus' cannot be greater than 100.");
            }
            if (PODAttachment != null && PODAttachment.Length > 100)
            {
                __errors.Add("Length of property 'PODAttachment' cannot be greater than 100.");
            }
            if (Comments != null && Comments.Length > 100)
            {
                __errors.Add("Length of property 'Comments' cannot be greater than 100.");
            }
            if (throwException && __errors.Any())
            {
                throw new zAppDev.DotNet.Framework.Exceptions.BusinessException("An instance of TypeClass 'ProofOfDelivery' has validation errors:\r\n\r\n" + string.Join("\r\n", __errors));
            }
            return __errors;
        }

        public virtual int _GetUniqueIdentifier()
        {
            var hashCode = 399326290;
            hashCode = hashCode * -1521134295 + (Id?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (DateIssued?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (PODNumber?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (QualityStatus?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (QuantityStatus?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (DeliveryStatus?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (PODAttachment?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Comments?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (DamagedPackaging.GetHashCode() );
            hashCode = hashCode * -1521134295 + (DamagedProducts.GetHashCode() );
            hashCode = hashCode * -1521134295 + (CloseToExpiry.GetHashCode() );
            return hashCode;
        }






/// <summary>
/// Copies the current object to a new instance
/// </summary>
/// <param name="deep">Copy members that refer to objects external to this class (not dependent)</param>
/// <param name="copiedObjects">Objects that should be reused</param>
/// <param name="asNew">Copy the current object as a new one, ready to be persisted, along all its members.</param>
/// <param name="reuseNestedObjects">If asNew is true, this flag if set, forces the reuse of all external objects.</param>
/// <param name="copy">Optional - An existing [ProofOfDelivery] instance to use as the destination.</param>
/// <returns>A copy of the object</returns>
        public virtual ProofOfDelivery Copy(bool deep=false, Hashtable copiedObjects=null, bool asNew=false, bool reuseNestedObjects = false, ProofOfDelivery copy = null)
        {
            if(copiedObjects == null)
            {
                copiedObjects = new Hashtable();
            }
            if (copy == null && copiedObjects.Contains(this))
                return (ProofOfDelivery)copiedObjects[this];
            copy = copy ?? new ProofOfDelivery();
            if (!asNew)
            {
                copy.TransientId = this.TransientId;
                copy.Id = this.Id;
            }
            copy.DateIssued = this.DateIssued;
            copy.PODNumber = this.PODNumber;
            copy.QualityStatus = this.QualityStatus;
            copy.QuantityStatus = this.QuantityStatus;
            copy.DeliveryStatus = this.DeliveryStatus;
            copy.PODAttachment = this.PODAttachment;
            copy.Comments = this.Comments;
            copy.DamagedPackaging = this.DamagedPackaging;
            copy.DamagedProducts = this.DamagedProducts;
            copy.CloseToExpiry = this.CloseToExpiry;
            if (!copiedObjects.Contains(this))
            {
                copiedObjects.Add(this, copy);
            }
            if(deep && this.truckDriver != null)
            {
                if (!copiedObjects.Contains(this.truckDriver))
                {
                    if (asNew && reuseNestedObjects)
                        copy.TruckDriver = this.TruckDriver;
                    else if (asNew)
                        copy.TruckDriver = this.TruckDriver.Copy(deep, copiedObjects, true);
                    else
                        copy.truckDriver = this.truckDriver.Copy(deep, copiedObjects, false);
                }
                else
                {
                    if (asNew)
                        copy.TruckDriver = (TruckDriver)copiedObjects[this.TruckDriver];
                    else
                        copy.truckDriver = (TruckDriver)copiedObjects[this.TruckDriver];
                }
            }
            if(deep && this.orderWMS != null)
            {
                if (!copiedObjects.Contains(this.orderWMS))
                {
                    if (asNew && reuseNestedObjects)
                        copy.OrderWMS = this.OrderWMS;
                    else if (asNew)
                        copy.OrderWMS = this.OrderWMS.Copy(deep, copiedObjects, true);
                    else
                        copy.orderWMS = this.orderWMS.Copy(deep, copiedObjects, false);
                }
                else
                {
                    if (asNew)
                        copy.OrderWMS = (OrderWMS)copiedObjects[this.OrderWMS];
                    else
                        copy.orderWMS = (OrderWMS)copiedObjects[this.OrderWMS];
                }
            }
            return copy;
        }

        public override bool Equals(object obj)
        {
            var compareTo = obj as ProofOfDelivery;
            if (ReferenceEquals(this, compareTo))
            {
                return true;
            }
            if (compareTo == null || !this.GetType().Equals(compareTo.GetTypeUnproxied()))
            {
                return false;
            }
            if (this.HasSameNonDefaultIdAs(compareTo))
            {
                return true;
            }
            // Since the Ids aren't the same, both of them must be transient to
            // compare domain signatures; because if one is transient and the
            // other is a persisted entity, then they cannot be the same object.
            return this.IsTransient() && compareTo.IsTransient() && (base.Equals(compareTo) || this.TransientId.Equals(compareTo.TransientId));
        }

// Maintain equality operator semantics for entities.
        public static bool operator ==(ProofOfDelivery x, ProofOfDelivery y)
        {
            // By default, == and Equals compares references. In order to
            // maintain these semantics with entities, we need to compare by
            // identity value. The Equals(x, y) override is used to guard
            // against null values; it then calls EntityEquals().
            return Equals(x, y);
        }

// Maintain inequality operator semantics for entities.
        public static bool operator !=(ProofOfDelivery x, ProofOfDelivery y)
        {
            return !(x == y);
        }

        private PropertyInfo __propertyKeyCache;
        public virtual PropertyInfo GetPrimaryKey()
        {
            if (__propertyKeyCache == null)
            {
                __propertyKeyCache = this.GetType().GetProperty("Id");
            }
            return __propertyKeyCache;
        }


/// <summary>
///     To help ensure hashcode uniqueness, a carefully selected random number multiplier
///     is used within the calculation.  Goodrich and Tamassia's Data Structures and
///     Algorithms in Java asserts that 31, 33, 37, 39 and 41 will produce the fewest number
///     of collissions.  See http://computinglife.wordpress.com/2008/11/20/why-do-hash-functions-use-prime-numbers/
///     for more information.
/// </summary>
        private const int HashMultiplier = 31;
        private int? cachedHashcode;

        public override int GetHashCode()
        {
            if (this.cachedHashcode.HasValue)
            {
                return this.cachedHashcode.Value;
            }
            if (this.IsTransient())
            {
                //this.cachedHashcode = base.GetHashCode();
                return this.TransientId.GetHashCode(); //don't cache because this won't stay transient forever
            }
            else
            {
                unchecked
                {
                    // It's possible for two objects to return the same hash code based on
                    // identically valued properties, even if they're of two different types,
                    // so we include the object's type in the hash calculation
                    var hashCode = this.GetType().GetHashCode();
                    this.cachedHashcode = (hashCode * HashMultiplier) ^ this.Id.GetHashCode();
                }
            }
            return this.cachedHashcode.Value;
        }

/// <summary>
///     Transient objects are not associated with an item already in storage.  For instance,
///     a Customer is transient if its Id is 0.  It's virtual to allow NHibernate-backed
///     objects to be lazily loaded.
/// </summary>
        public virtual bool IsTransient()
        {
            return this.Id == default(int) || this.Id.Equals(default(int));
        }

/// <summary>
///     When NHibernate proxies objects, it masks the type of the actual entity object.
///     This wrapper burrows into the proxied object to get its actual type.
///
///     Although this assumes NHibernate is being used, it doesn't require any NHibernate
///     related dependencies and has no bad side effects if NHibernate isn't being used.
///
///     Related discussion is at http://groups.google.com/group/sharp-architecture/browse_thread/thread/ddd05f9baede023a ...thanks Jay Oliver!
/// </summary>
        protected virtual System.Type GetTypeUnproxied()
        {
            return this.GetType();
        }

/// <summary>
///     Returns true if self and the provided entity have the same Id values
///     and the Ids are not of the default Id value
/// </summary>
        protected bool HasSameNonDefaultIdAs(ProofOfDelivery compareTo)
        {
            return !this.IsTransient() && !compareTo.IsTransient() && this.Id.Equals(compareTo.Id);
        }

        #endregion


    }
}
