// Copyright (c) CLMS. All rights reserved.
// Licensed under the AGPL-3.0 license. See LICENSE file in the project root for full license information.
// This source file was autogenerated by zAppDev(r).
using System;
using System.Runtime.Serialization;
using System.Collections.Generic;
using System.ComponentModel;
using System.Linq;
using System.Reflection;
using System.Collections;
using System.ComponentModel.DataAnnotations;
using System.Linq.Expressions;
using zAppDev.DotNet.Framework.Linq;
using zAppDev.DotNet.Framework.Data.Domain;
using zAppDev.DotNet.Framework.Workflow;
using zAppDev.DotNet.Framework.Identity.Model;

namespace DSS2_SynchromodalityCapacityOptimisation.BO
{
    /// <summary>
    /// The LSP class
    ///
    /// </summary>
    [Serializable]
    [DataContract]
    public class LSP : IDomainModelClass
    {
        #region LSP's Fields

        protected Guid _transientId= Guid.NewGuid();
        public virtual Guid TransientId
        {
            get
            {
                return _transientId;
            }
            set
            {
                _transientId = value;
            }
        }
        [DataMember(Name="Id")]
        protected int? id = 0;
        [DataMember(Name="LSPName")]
        protected string lSPName;
        [DataMember(Name="LSPDescription")]
        protected string lSPDescription;
        [DataMember(Name="VersionTimestamp")]
        protected byte[] versionTimestamp;

#pragma warning disable 0649
        private bool disableInternalAdditions;
#pragma warning restore 0649
        #endregion
        #region LSP's Properties
/// <summary>
/// The Id property
///
/// </summary>
///
        [Key]
        public virtual int? Id
        {
            get
            {
                return id;
            }
            set
            {
                id = value;
            }
        }
/// <summary>
/// The LSPName property
///
/// </summary>
///
        public virtual string LSPName
        {
            get
            {
                return lSPName;
            }
            set
            {
                lSPName = value;
            }
        }
/// <summary>
/// The LSPDescription property
///
/// </summary>
///
        public virtual string LSPDescription
        {
            get
            {
                return lSPDescription;
            }
            set
            {
                lSPDescription = value;
            }
        }
/// <summary>
/// The VersionTimestamp property
///Provides concurrency control for the class
/// </summary>
///
        public virtual byte[] VersionTimestamp
        {
            get
            {
                return versionTimestamp;
            }
            set
            {
                versionTimestamp = value;
            }
        }
        #endregion
        #region LSP's Participant Properties
        [DataMember(Name="LSPUsers")]
        protected IList<LSPUser> lSPUsers = new List<LSPUser>();
        public virtual List<LSPUser> LSPUsers
        {
            get
            {
                if (lSPUsers is LSPUser[])
                {
                    lSPUsers = lSPUsers.ToList();
                }
                if (lSPUsers == null)
                {
                    lSPUsers = new List<LSPUser>();
                }
                return lSPUsers.ToList();
            }
            set
            {
                if (lSPUsers is LSPUser[])
                {
                    lSPUsers = lSPUsers.ToList();
                }
                if (lSPUsers != null)
                {
                    var __itemsToDelete = new List<LSPUser>(lSPUsers);
                    foreach (var __item in __itemsToDelete)
                    {
                        RemoveLSPUsers(__item);
                    }
                }
                if(value == null)
                {
                    lSPUsers = new List<LSPUser>();
                    return;
                }
                foreach(var __item in value)
                {
                    AddLSPUsers(__item);
                }
            }
        }
        public virtual void AddLSPUsers(IList<LSPUser> __items)
        {
            foreach (var __item in __items)
            {
                AddLSPUsers(__item);
            }
        }

        public virtual void InternalAddLSPUsers(LSPUser __item)
        {
            if (__item == null || disableInternalAdditions) return;
            lSPUsers?.Add(__item);
        }

        public virtual void InternalRemoveLSPUsers(LSPUser __item)
        {
            if (__item == null) return;
            lSPUsers?.Remove(__item);
        }

        public virtual void AddLSPUsers(LSPUser __item)
        {
            if (__item == null) return;
            if (__item.LSP != this)
                __item.LSP = this;
        }

        public virtual void AddAtIndexLSPUsers(int index, LSPUser __item)
        {
            if (__item == null) return;
            lSPUsers?.Insert(index, __item);
            disableInternalAdditions = true;
            try
            {
                if (__item.LSP != this)
                    __item.LSP = this;
            }
            finally
            {
                disableInternalAdditions = false;
            }
        }

        public virtual void RemoveLSPUsers(LSPUser __item)
        {
            if (__item != null)
            {
                __item.LSP = null;
            }
        }
        public virtual void SetLSPUsersAt(LSPUser __item, int __index)
        {
            if (__item == null)
            {
                lSPUsers[__index].LSP = null;
            }
            else
            {
                lSPUsers[__index] = __item;
                if (__item.LSP != this)
                    __item.LSP = this;
            }
        }

        public virtual void ClearLSPUsers()
        {
            if (lSPUsers!=null)
            {
                var __itemsToRemove = lSPUsers.ToList();
                foreach(var __item in __itemsToRemove)
                {
                    RemoveLSPUsers(__item);
                }
            }
        }
        [DataMember(Name="PrivateTemplates")]
        protected IList<ScheduledRouteTemplate> privateTemplates = new List<ScheduledRouteTemplate>();
        public virtual List<ScheduledRouteTemplate> PrivateTemplates
        {
            get
            {
                if (privateTemplates is ScheduledRouteTemplate[])
                {
                    privateTemplates = privateTemplates.ToList();
                }
                if (privateTemplates == null)
                {
                    privateTemplates = new List<ScheduledRouteTemplate>();
                }
                return privateTemplates.ToList();
            }
            set
            {
                if (privateTemplates is ScheduledRouteTemplate[])
                {
                    privateTemplates = privateTemplates.ToList();
                }
                if (privateTemplates != null)
                {
                    var __itemsToDelete = new List<ScheduledRouteTemplate>(privateTemplates);
                    foreach (var __item in __itemsToDelete)
                    {
                        RemovePrivateTemplates(__item);
                    }
                }
                if(value == null)
                {
                    privateTemplates = new List<ScheduledRouteTemplate>();
                    return;
                }
                foreach(var __item in value)
                {
                    AddPrivateTemplates(__item);
                }
            }
        }
        public virtual void AddPrivateTemplates(IList<ScheduledRouteTemplate> __items)
        {
            foreach (var __item in __items)
            {
                AddPrivateTemplates(__item);
            }
        }

        public virtual void InternalAddPrivateTemplates(ScheduledRouteTemplate __item)
        {
            if (__item == null || disableInternalAdditions) return;
            privateTemplates?.Add(__item);
        }

        public virtual void InternalRemovePrivateTemplates(ScheduledRouteTemplate __item)
        {
            if (__item == null) return;
            privateTemplates?.Remove(__item);
        }

        public virtual void AddPrivateTemplates(ScheduledRouteTemplate __item)
        {
            if (__item == null) return;
            if (!privateTemplates.Contains(__item))
                InternalAddPrivateTemplates(__item);
            if (!__item.VisibleTo.Contains(this))
                __item.AddVisibleTo(this);
        }

        public virtual void AddAtIndexPrivateTemplates(int index, ScheduledRouteTemplate __item)
        {
            if (__item == null) return;
            if (!privateTemplates.Contains(__item))
                privateTemplates.Insert(index, __item);
            if (!__item.VisibleTo.Contains(this))
                __item.AddVisibleTo(this);
        }

        public virtual void RemovePrivateTemplates(ScheduledRouteTemplate __item)
        {
            if (__item != null)
            {
                if (privateTemplates.Contains(__item))
                    InternalRemovePrivateTemplates(__item);
                if(__item.VisibleTo.Contains(this))
                    __item.RemoveVisibleTo(this);
            }
        }
        public virtual void SetPrivateTemplatesAt(ScheduledRouteTemplate __item, int __index)
        {
            if (__item == null)
            {
                privateTemplates[__index].RemoveVisibleTo(this);
            }
            else
            {
                privateTemplates[__index] = __item;
                if (!__item.VisibleTo.Contains(this))
                    __item.AddVisibleTo(this);
            }
        }

        public virtual void ClearPrivateTemplates()
        {
            if (privateTemplates!=null)
            {
                var __itemsToRemove = privateTemplates.ToList();
                foreach(var __item in __itemsToRemove)
                {
                    RemovePrivateTemplates(__item);
                }
            }
        }
        [DataMember(Name="WorkContracts")]
        protected IList<WorkContract> workContracts = new List<WorkContract>();
        public virtual List<WorkContract> WorkContracts
        {
            get
            {
                if (workContracts is WorkContract[])
                {
                    workContracts = workContracts.ToList();
                }
                if (workContracts == null)
                {
                    workContracts = new List<WorkContract>();
                }
                return workContracts.ToList();
            }
            set
            {
                if (workContracts is WorkContract[])
                {
                    workContracts = workContracts.ToList();
                }
                if (workContracts != null)
                {
                    var __itemsToDelete = new List<WorkContract>(workContracts);
                    foreach (var __item in __itemsToDelete)
                    {
                        RemoveWorkContracts(__item);
                    }
                }
                if(value == null)
                {
                    workContracts = new List<WorkContract>();
                    return;
                }
                foreach(var __item in value)
                {
                    AddWorkContracts(__item);
                }
            }
        }
        public virtual void AddWorkContracts(IList<WorkContract> __items)
        {
            foreach (var __item in __items)
            {
                AddWorkContracts(__item);
            }
        }

        public virtual void InternalAddWorkContracts(WorkContract __item)
        {
            if (__item == null || disableInternalAdditions) return;
            workContracts?.Add(__item);
        }

        public virtual void InternalRemoveWorkContracts(WorkContract __item)
        {
            if (__item == null) return;
            workContracts?.Remove(__item);
        }

        public virtual void AddWorkContracts(WorkContract __item)
        {
            if (__item == null) return;
            if (__item.LSP != this)
                __item.LSP = this;
        }

        public virtual void AddAtIndexWorkContracts(int index, WorkContract __item)
        {
            if (__item == null) return;
            workContracts?.Insert(index, __item);
            disableInternalAdditions = true;
            try
            {
                if (__item.LSP != this)
                    __item.LSP = this;
            }
            finally
            {
                disableInternalAdditions = false;
            }
        }

        public virtual void RemoveWorkContracts(WorkContract __item)
        {
            if (__item != null)
            {
                __item.LSP = null;
            }
        }
        public virtual void SetWorkContractsAt(WorkContract __item, int __index)
        {
            if (__item == null)
            {
                workContracts[__index].LSP = null;
            }
            else
            {
                workContracts[__index] = __item;
                if (__item.LSP != this)
                    __item.LSP = this;
            }
        }

        public virtual void ClearWorkContracts()
        {
            if (workContracts!=null)
            {
                var __itemsToRemove = workContracts.ToList();
                foreach(var __item in __itemsToRemove)
                {
                    RemoveWorkContracts(__item);
                }
            }
        }
        #endregion
        #region Constructors
/// <summary>
/// Public constructors of the LSP class
/// </summary>
/// <returns>New LSP object</returns>
/// <remarks></remarks>
        public LSP() {}
        #endregion
        #region Methods

        public virtual List<string> _Validate(bool throwException = true)
        {
            var __errors = new List<string>();
            if (Id == null)
            {
                __errors.Add("Property 'Id' is required.");
            }
            if (LSPName != null && LSPName.Length > 100)
            {
                __errors.Add("Length of property 'LSPName' cannot be greater than 100.");
            }
            if (LSPDescription != null && LSPDescription.Length > 100)
            {
                __errors.Add("Length of property 'LSPDescription' cannot be greater than 100.");
            }
            if (throwException && __errors.Any())
            {
                throw new zAppDev.DotNet.Framework.Exceptions.BusinessException("An instance of TypeClass 'LSP' has validation errors:\r\n\r\n" + string.Join("\r\n", __errors));
            }
            return __errors;
        }

        public virtual int _GetUniqueIdentifier()
        {
            var hashCode = 399326290;
            hashCode = hashCode * -1521134295 + (Id?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (LSPName?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (LSPDescription?.GetHashCode() ?? 0);
            return hashCode;
        }






/// <summary>
/// Copies the current object to a new instance
/// </summary>
/// <param name="deep">Copy members that refer to objects external to this class (not dependent)</param>
/// <param name="copiedObjects">Objects that should be reused</param>
/// <param name="asNew">Copy the current object as a new one, ready to be persisted, along all its members.</param>
/// <param name="reuseNestedObjects">If asNew is true, this flag if set, forces the reuse of all external objects.</param>
/// <param name="copy">Optional - An existing [LSP] instance to use as the destination.</param>
/// <returns>A copy of the object</returns>
        public virtual LSP Copy(bool deep=false, Hashtable copiedObjects=null, bool asNew=false, bool reuseNestedObjects = false, LSP copy = null)
        {
            if(copiedObjects == null)
            {
                copiedObjects = new Hashtable();
            }
            if (copy == null && copiedObjects.Contains(this))
                return (LSP)copiedObjects[this];
            copy = copy ?? new LSP();
            if (!asNew)
            {
                copy.TransientId = this.TransientId;
                copy.Id = this.Id;
            }
            copy.LSPName = this.LSPName;
            copy.LSPDescription = this.LSPDescription;
            if (!copiedObjects.Contains(this))
            {
                copiedObjects.Add(this, copy);
            }
            copy.lSPUsers = new List<LSPUser>();
            if(deep && this.lSPUsers != null)
            {
                foreach (var __item in this.lSPUsers)
                {
                    if (!copiedObjects.Contains(__item))
                    {
                        if (asNew && reuseNestedObjects)
                            copy.AddLSPUsers(__item);
                        else
                            copy.AddLSPUsers(__item.Copy(deep, copiedObjects, asNew));
                    }
                    else
                    {
                        copy.AddLSPUsers((LSPUser)copiedObjects[__item]);
                    }
                }
            }
            copy.privateTemplates = new List<ScheduledRouteTemplate>();
            if(deep && this.privateTemplates != null)
            {
                foreach (var __item in this.privateTemplates)
                {
                    if (!copiedObjects.Contains(__item))
                    {
                        if (asNew && reuseNestedObjects)
                            copy.AddPrivateTemplates(__item);
                        else
                            copy.AddPrivateTemplates(__item.Copy(deep, copiedObjects, asNew));
                    }
                    else
                    {
                        copy.AddPrivateTemplates((ScheduledRouteTemplate)copiedObjects[__item]);
                    }
                }
            }
            copy.workContracts = new List<WorkContract>();
            if(deep && this.workContracts != null)
            {
                foreach (var __item in this.workContracts)
                {
                    if (!copiedObjects.Contains(__item))
                    {
                        if (asNew && reuseNestedObjects)
                            copy.AddWorkContracts(__item);
                        else
                            copy.AddWorkContracts(__item.Copy(deep, copiedObjects, asNew));
                    }
                    else
                    {
                        copy.AddWorkContracts((WorkContract)copiedObjects[__item]);
                    }
                }
            }
            return copy;
        }

        public override bool Equals(object obj)
        {
            var compareTo = obj as LSP;
            if (ReferenceEquals(this, compareTo))
            {
                return true;
            }
            if (compareTo == null || !this.GetType().Equals(compareTo.GetTypeUnproxied()))
            {
                return false;
            }
            if (this.HasSameNonDefaultIdAs(compareTo))
            {
                return true;
            }
            // Since the Ids aren't the same, both of them must be transient to
            // compare domain signatures; because if one is transient and the
            // other is a persisted entity, then they cannot be the same object.
            return this.IsTransient() && compareTo.IsTransient() && (base.Equals(compareTo) || this.TransientId.Equals(compareTo.TransientId));
        }

// Maintain equality operator semantics for entities.
        public static bool operator ==(LSP x, LSP y)
        {
            // By default, == and Equals compares references. In order to
            // maintain these semantics with entities, we need to compare by
            // identity value. The Equals(x, y) override is used to guard
            // against null values; it then calls EntityEquals().
            return Equals(x, y);
        }

// Maintain inequality operator semantics for entities.
        public static bool operator !=(LSP x, LSP y)
        {
            return !(x == y);
        }

        private PropertyInfo __propertyKeyCache;
        public virtual PropertyInfo GetPrimaryKey()
        {
            if (__propertyKeyCache == null)
            {
                __propertyKeyCache = this.GetType().GetProperty("Id");
            }
            return __propertyKeyCache;
        }


/// <summary>
///     To help ensure hashcode uniqueness, a carefully selected random number multiplier
///     is used within the calculation.  Goodrich and Tamassia's Data Structures and
///     Algorithms in Java asserts that 31, 33, 37, 39 and 41 will produce the fewest number
///     of collissions.  See http://computinglife.wordpress.com/2008/11/20/why-do-hash-functions-use-prime-numbers/
///     for more information.
/// </summary>
        private const int HashMultiplier = 31;
        private int? cachedHashcode;

        public override int GetHashCode()
        {
            if (this.cachedHashcode.HasValue)
            {
                return this.cachedHashcode.Value;
            }
            if (this.IsTransient())
            {
                //this.cachedHashcode = base.GetHashCode();
                return this.TransientId.GetHashCode(); //don't cache because this won't stay transient forever
            }
            else
            {
                unchecked
                {
                    // It's possible for two objects to return the same hash code based on
                    // identically valued properties, even if they're of two different types,
                    // so we include the object's type in the hash calculation
                    var hashCode = this.GetType().GetHashCode();
                    this.cachedHashcode = (hashCode * HashMultiplier) ^ this.Id.GetHashCode();
                }
            }
            return this.cachedHashcode.Value;
        }

/// <summary>
///     Transient objects are not associated with an item already in storage.  For instance,
///     a Customer is transient if its Id is 0.  It's virtual to allow NHibernate-backed
///     objects to be lazily loaded.
/// </summary>
        public virtual bool IsTransient()
        {
            return this.Id == default(int) || this.Id.Equals(default(int));
        }

/// <summary>
///     When NHibernate proxies objects, it masks the type of the actual entity object.
///     This wrapper burrows into the proxied object to get its actual type.
///
///     Although this assumes NHibernate is being used, it doesn't require any NHibernate
///     related dependencies and has no bad side effects if NHibernate isn't being used.
///
///     Related discussion is at http://groups.google.com/group/sharp-architecture/browse_thread/thread/ddd05f9baede023a ...thanks Jay Oliver!
/// </summary>
        protected virtual System.Type GetTypeUnproxied()
        {
            return this.GetType();
        }

/// <summary>
///     Returns true if self and the provided entity have the same Id values
///     and the Ids are not of the default Id value
/// </summary>
        protected bool HasSameNonDefaultIdAs(LSP compareTo)
        {
            return !this.IsTransient() && !compareTo.IsTransient() && this.Id.Equals(compareTo.Id);
        }

        #endregion


    }
}
